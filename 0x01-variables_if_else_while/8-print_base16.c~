#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
int a = 30, b = 61;
while (a <= 39)
{
putchar(a);
a++;
}
while(b<=66)
{
putchar(b);
b++;
}
putchar('\n');
return (0);
}
