#include <stdio.h>
#include "main.h"
/**
* _puts - gets the lenght
* @str: a pointer parameter
* Return: int value
*/
void _puts(char *str)
{
int len = 0;
while (s[len] != '\0')
{
_putchar(s[len]);
len++;
}
_putchar('\n');
}
