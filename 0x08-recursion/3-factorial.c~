#include <stdio.h>
#include "main.h"
/**
* factorial - gets the lenght
* @n: a pointer parameter
* Return: int
*/
int factorial(int n)
{
if (n == 1 || n == 0)
return (1);
return n * factorial(n - 1);
}
