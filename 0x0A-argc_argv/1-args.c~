#include <stdio.h>
#include "main.h"
/**
* _pow_recursion - gets the lenght
* @argv: arg vector
* @argc: arg count
* Return: int
*/
int main(int argc, char* argv [])
{
printf("%s\n", argv[0]);
return (0);
}
